

# Free-tier API key for Google's Gemini vision model, used as the fallback
# identification step when barcode + OCR aren't enough. Get one at
# https://aistudio.google.com/app/apikey (verify current free-tier limits
# there, since they can change).
GEMINI_API_KEY=your_gemini_api_key_here
